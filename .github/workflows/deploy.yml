# DISABLED — deploy.yml previously overwrote the live zyrvath.com static site.
# The live site at zyrvath.com is maintained separately on the gh-pages branch.
# Do NOT re-enable automatic deployment without confirming what is on gh-pages.

name: Deploy Zyrvath (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type CONFIRM to deploy (WARNING: will overwrite gh-pages)'
        required: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'CONFIRM'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: This workflow is disabled
        run: echo "Deployment is manually disabled to protect zyrvath.com"
